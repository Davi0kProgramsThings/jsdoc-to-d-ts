ES2022 <: JSDOC {
    File = (Class | ~Class any)*
    Class = DocComment? Export? "class" id "{" ClassBody "}"
    Export = "export" "default"?
    ClassBody = Method*
    Method = DocComment? "async"? id "(" ArgumentList ")" "{" (~"}" any)* "}"
    ArgumentList = ListOf<id, ",">
}
